import React from 'react';
import TextBar from '../../components/common/bar/TextBar';

const Members = () => {
  return (
    <div>
      <TextBar title={'Members'} back={'back'} setting={'setting'} />
    </div>
  );
};

export default Members;
