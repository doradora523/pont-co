import React from 'react';
import TextBar from '../../components/common/bar/TextBar';

const Playing = () => {
  return (
    <div>
      <TextBar title={'Members'} back={'back'} />
    </div>
  );
};

export default Playing;
